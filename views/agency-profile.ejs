<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= pageTitle || 'Agency Profile' %></title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .profile-container {
      max-width: 900px;
      margin: 100px auto;
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0 0 25px rgba(0, 0, 0, 0.08);
      background-color: #fff;
      font-family: 'Segoe UI', sans-serif;
      transform: translateY(-20px) scale(0.98);
      transition: transform 0.3s ease-in-out;
      
    }

    .profile-header {
      text-align: center;
      margin-bottom: 30px;
    }

    .agency-logo {
      width: 140px;
      height: 140px;
      object-fit: cover;
      border-radius: 50%;
      border: 3px solid #ddd;
      margin-bottom: 15px;
    }

    .profile-details {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 25px;
      margin-top: 20px;
    }

    .detail-box {
      background-color: #f7f7f7;
      padding: 18px 20px;
      border-radius: 10px;
      box-shadow: inset 0 0 6px rgba(0, 0, 0, 0.02);
    }

    .detail-box h4 {
      margin: 0;
      font-size: 14px;
      color: #777;
    }

    .detail-box p {
      margin: 5px 0 0;
      font-size: 17px;
      color: #222;
      font-weight: 500;
    }

    .edit-btn {
      display: inline-block;
      margin: 30px auto 0;
      padding: 12px 28px;
      background-color: green;
      color: #fff;
      text-decoration: none;
      font-size: 16px;
      border-radius: 8px;
      transition: 0.3s ease;
    }

    .edit-btn:hover {
      background-color: black;
      color: #fff;
      transform: translateY(-2px);
      box-shadow: 0 4px 15px rgba(34, 197, 94, 0.2);
      
    }

    @media (max-width: 768px) {
      .profile-details {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>

<%- include('partials/header', { user: user }) %>

<div class="profile-container">
  <div class="profile-header">
    <% if (user.agencyLogo) { %>
      <img src="<%= user.agencyLogo %>" alt="Agency Logo" class="agency-logo">
    <% } else { %>
      <img src="/images/default-logo.png" alt="No Logo" class="agency-logo">
    <% } %>
    <h2><%= user.agencyName %></h2>
    <p><strong>Registered Email:</strong> <%= user.email %></p>
  </div>

  <div class="profile-details">
    <div class="detail-box">
      <h4>Agency Address</h4>
      <p><%= user.agencyAddress || 'N/A' %></p>
    </div>
    <div class="detail-box">
      <h4>Contact Number</h4>
      <p><%= user.phone || 'N/A' %></p>
    </div>
    <div class="detail-box">
      <h4>Account Type</h4>
      <p><%= user.role %></p>
    </div>
    <div class="detail-box">
      <h4>Name of Owner</h4>
      <p><%= user.name %></p>
    </div>
  </div>

  <div style="text-align: center;">
    <a href="/agency/edit-profile" class="edit-btn">Edit Profile</a>
  </div>
</div>

<%- include('partials/footer') %>

</body>
</html>